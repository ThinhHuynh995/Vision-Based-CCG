# ─────────────────────────────────────────────────────────────
# Vision-Based Abnormal Behavior Detection
# requirements.txt — cài bằng: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Core vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Deep learning (CPU-only — bỏ +cpu nếu dùng GPU)
torch>=2.0.0
torchvision>=0.15.0

# Object detection
ultralytics>=8.0.0       # YOLOv8

# Augmentation (tùy chọn nhưng khuyến khích)
albumentations>=1.3.0

# Config + utils
pyyaml>=6.0
scipy>=1.11.0

# Visualization & reporting
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: Web UI
# gradio>=4.0.0

# Dev & testing
pytest>=7.0.0
